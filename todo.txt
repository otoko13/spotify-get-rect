Restyle the dropdown no rounded edges, match the blur on the header (dropdown component for reuse) 

Text size on mobile for header based on text length

Caret dropdown when on mobile

Highlight in babylon 

Try moving the ai and profile routes into the individual pages to prevent reloads of the data
